package com.sky.controllers;


/*@Component
public class ClassChanger implements Converter<String, Point>{

	@Override
	public Point convert(String source) {
		Point p = null;
		if (source != null && source.split("-").length == 2) {
			String[] strs = source.split("-");
			p = new Point(Double.parseDouble(strs[0]), Double.parseDouble(strs[1]));
		}
		return p;
	}
}*/
